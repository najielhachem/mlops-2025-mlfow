[project]
name = "mlflow-tutorial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "mlflow==2.22.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
